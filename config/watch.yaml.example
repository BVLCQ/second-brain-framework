# Second Brain — o universo observável pelo cérebro (INSTÂNCIA)
# Copie para config/watch.yaml · tudo enumerado por você: o que não está
# listado, o cérebro não vê. Tokens read-only ficam em config/.env.

github:
  - owner/repo-um          # repos a observar: PRs, issues, reviews
  - owner/repo-dois

jira:
  me: true                 # seus cards (assignee = você)
  following: true          # cards que você segue (watcher)
  boards: []               # boards do time, INTEIROS (project key ou board id)

gmail:
  window: daily            # digest do dia anterior (OAuth — docs/google-oauth.md)

slack:
  channels: []             # ex.: ["#retention", "#squad-cs"] — noturno
  users: []                # ex.: ["@ana"] — noturno
  default_window: 7d       # janela padrão do /slack sob demanda

gdocs:                      # docs vivos (OAuth — docs/google-oauth.md)
  - id: ""                 # id do doc no Drive (parte da URL)
    name: ""               # nome humano p/ nomear snapshots

schedule: nightly           # conectores rodam junto com o /update noturno
