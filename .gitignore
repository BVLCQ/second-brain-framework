# ═══ ZONAS DE DADOS (a instância vive no mesmo clone — o git NUNCA vê seus dados) ═══
/RAW/
/PROCESSED/
/FRESH/
/PROFILE/
/PLANNING/
RAW/
PROCESSED/
FRESH/
PROFILE/
PLANNING/

# ═══ Config e estado da INSTÂNCIA (nunca commitados) ═══
.env
config/.env
config/watch.yaml
config/.state.json
config/nightly.log

# Sistema
.DS_Store
__pycache__/
*.pyc
